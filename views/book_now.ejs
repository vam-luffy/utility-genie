<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Now</title>
    <link rel="stylesheet" href="/styles/style.css">
    <style>
        /* Additional Styling for Book Now Page */
        .book-now-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .provider-details {
            margin-bottom: 20px;
        }
        .provider-details h2 {
            margin: 0;
            color: #333;
        }
        .booking-form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        .booking-form input, .booking-form textarea {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
            width: 100%;
        }
        .booking-form button {
            padding: 12px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
        }
        .booking-form button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <main>
        <div class="book-now-container">
            <div class="provider-details">
                <h2>Book <%= provider.username %></h2>
                <p><strong>Profession:</strong> <%= provider.profession %></p>
                <p><strong>Email:</strong> <%= provider.email %></p>
            </div>

            <form class="booking-form" action="/book-now/<%= provider.username %>" method="POST">
                <label for="address">Address:</label>
                <input type="text" id="address" name="address" required placeholder="Enter the service address">
                
                <label for="work_description">Work Description:</label>
                <textarea id="work_description" name="work_description" rows="4" required placeholder="Describe the work to be done..."></textarea>
                
                <label for="date">Preferred Date:</label>
                <input type="date" id="date" name="date" required>
                
                <label for="time">Preferred Time:</label>
                <input type="time" id="time" name="time" required>
                
                <button type="submit">Confirm Booking</button>
            </form>
        </div>
    </main>
</body>
</html>
